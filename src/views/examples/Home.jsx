import React from 'react'

import SectionTitle from '../../components/SectionTitle'

export default props => {

    return (
        
        <SectionTitle title='Aprendendo React' description='Testando os conceitos básicos com React'/>
    )
}